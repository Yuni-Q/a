<%= form_for(@categorie3) do |f| %>
  <% if @categorie3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorie3.errors.count, "error") %> prohibited this categorie3 from being saved:</h2>

      <ul>
      <% @categorie3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
