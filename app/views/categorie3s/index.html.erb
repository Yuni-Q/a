<p id="notice"><%= notice %></p>

<h1>Listing Categorie3s</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categorie3s.each do |categorie3| %>
      <tr>
        <td><%= link_to 'Show', categorie3 %></td>
        <td><%= link_to 'Edit', edit_categorie3_path(categorie3) %></td>
        <td><%= link_to 'Destroy', categorie3, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Categorie3', new_categorie3_path %>
