<%= form_for(@categorie1) do |f| %>
  <% if @categorie1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorie1.errors.count, "error") %> prohibited this categorie1 from being saved:</h2>

      <ul>
      <% @categorie1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
